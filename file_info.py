class FileInfo:

    def __init__(self, filename, cur_folder, destination, data=''):
        self.data = data
        self.filename = filename
        self.current_folder = cur_folder
        self.destination = destination
